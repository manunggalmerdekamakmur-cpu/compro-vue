{
  "name": "manunggal",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "prebuild": "rm -rf dist && npm run analyze",
    "analyze": "vite-bundle-analyzer",
    "build:prod": "NODE_ENV=production vite build",
    "build:mobile": "vite build --mode mobile",
    "deploy": "npm run build:prod && vercel --prod"
  },
  "dependencies": {
    "vue": "^3.3.4",
    "vue-router": "^4.2.4"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.2.3",
    "autoprefixer": "^10.4.16",
    "cssnano": "^6.0.1",
    "rollup-plugin-visualizer": "^5.12.0",
    "terser": "^5.26.0",
    "vite": "^4.5.14"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "browserslist": [
    "> 1%",
    "last 2 versions",
    "not dead"
  ]
}